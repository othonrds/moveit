import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // File the same folder.


ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
